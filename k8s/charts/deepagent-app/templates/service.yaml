apiVersion: v1
kind: Service
metadata:
  name: {{ include "deepagent-app.fullname" . }}
  labels: {{- include "deepagent-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "deepagent-app.selectorLabels" . | nindent 4 }}
